apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
stringData:
  {{ .Values.barista.secretKey }}: {{ .Values.barista.secretValue }}
  {{ .Values.wavecafe.secretKey }}: {{ .Values.wavecafe.secretValue }}
